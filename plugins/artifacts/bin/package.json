{
  "name": "@btrmnt/artifacts-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "btrmnt": "../btrmnt"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild src/cli.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/cli.js --banner:js='import { createRequire } from \"module\"; const require = createRequire(import.meta.url);'",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@btrmnt/artifact-types": "workspace:*",
    "simple-git": "^3.36.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
