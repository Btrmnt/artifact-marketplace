{
  "name": "artifact-marketplace",
  "private": true,
  "version": "0.0.0",
  "description": "Public Claude Code marketplace for the btrmnt artifacts plugin.",
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r --if-present run lint",
    "typecheck": "pnpm -r --if-present run typecheck"
  }
}
